# Ejemplo de archivo de variables de entorno para el servidor
# Copia este archivo a `.env` y reemplaza los valores con tus credenciales reales.

# Puerto del servidor
PORT=3000

# URL del cliente (CORS)
CLIENT_URL=http://localhost:5173

# Cadena de conexión a MongoDB (por defecto usa una local)
# MONGODB_URI=mongodb://localhost:27017/magneto_embeddable_tool

# API Key de Hugging Face (REEMPLAZA con tu clave real en el archivo .env, NO la subas a Git)
HF_API_KEY=hf_your_api_key_here
